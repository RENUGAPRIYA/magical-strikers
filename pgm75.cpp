#include <iostream>
#include <cmath>
 
using namespace std;
 
int main ()
{
   int a[i];
cout<<"enter array elements";
cin>>a[i];
int size = my_sizeof(arr)/my_sizeof(arr[0]);
if(size%2==0)
{
i=size/2;
i++;
a[i]={'*'};

}else
{
n=size/2;
int i = std::nint(n); 
a[i]={'*'};
}

    return 0;
}
